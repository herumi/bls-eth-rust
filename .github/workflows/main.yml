name: test
on: [push]

jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - run: sudo apt install nasm
    - run: git submodule update --init --recursive ; cargo test --verbose

